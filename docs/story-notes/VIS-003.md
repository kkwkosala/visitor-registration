# VIS-003 — Visit Request Form

## Objective
Unified create/edit form component. Blocks editing approved/rejected requests.

## Key Decisions
- Dual mode via input() signal — DRY, avoids near-identical component duplication
- minDateValidator prevents past dates at client layer (belt + DB CHECK)
- Non-pending guard shows error in-place rather than redirecting

## Tests Written First (TDD)
- 7 form tests before implementation

## Commit
303324d1a4d458ad203e305609be360948cd6e10

## Issues
Closes #4 — VIS-003 Submit visit request
Closes #5 — VIS-005 Block edit of approved/rejected
