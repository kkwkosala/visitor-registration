# VIS-006/007/008 — Admin Dashboard

## Objective
Admin views all requests, filters by status, approves/rejects with optional comment, sees live stats.

## Key Decisions
- computed() for stats — derived from allRequests signal, zero extra API calls
- Optimistic UI update on approve/reject — instant feedback, no re-fetch
- Admin identity via RLS EXISTS subquery on profiles — not JWT claims

## Tests Written First (TDD)
- 7 tests: loading, row count, filter, stats display, approve buttons, updateStatus call, empty state

## Commit
e416671b7575adacdfe944174766d0d1d51117a0

## Issues
Closes #7 — VIS-006 View all requests
Closes #8 — VIS-007 Approve/reject
Closes #9 — VIS-008 Filter by status
